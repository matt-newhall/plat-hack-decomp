#include "macros/btlcmd.inc"

    .data

_000:
    CompareVarToValue OPCODE_FLAG_NOT, BTLVAR_BATTLE_TYPE, BATTLE_TYPE_DOUBLES, _010
    UpdateVarFromVar OPCODE_SET, BTLVAR_LAST_BATTLER_ID, BTLVAR_ATTACKER
    UpdateVarFromVar OPCODE_SET, BTLVAR_ATTACKER, BTLVAR_MSG_BATTLER_TEMP
    CompareMonDataToValue OPCODE_EQU, BTLSCR_ATTACKER_PARTNER, BATTLEMON_CUR_HP, 0, _005
    CopyWithCostar
    // {0} copied {1}'s stat changes with {2}!
    PrintMessage BattleStrings_Text_CostarAnnounced_Ally, TAG_NICKNAME_NICKNAME_ABILITY, BTLSCR_ATTACKER, BTLSCR_ATTACKER_PARTNER, BTLSCR_ATTACKER
    Wait
    WaitButtonABTime 30
    UpdateVar OPCODE_SET, BTLVAR_SIDE_EFFECT_FLAGS_INDIRECT, MOVE_SIDE_EFFECT_ON_HIT|MOVE_SUBSCRIPT_PTR_PRINT_MESSAGE_AND_PLAY_ANIMATION

_005:
    UpdateVarFromVar OPCODE_SET, BTLVAR_ATTACKER, BTLVAR_LAST_BATTLER_ID

_010:
    End
